# ============================================================
#  Regime-Aware Trading Command Center – Environment Template
#  Copy this file to .env and fill in your values.
#  NEVER commit .env to version control.
# ============================================================

# ── Vertex AI (Authentication via Application Default Credentials) ────────────
# No API key required. Run once in your terminal:
#   gcloud auth application-default login
#   gcloud auth application-default set-quota-project YOUR_PROJECT_ID

# Your GCP Project ID (find at console.cloud.google.com)
GOOGLE_CLOUD_PROJECT=your-gcp-project-id-here

# Region – us-central1 has the widest Gemini model availability on Vertex AI
GOOGLE_CLOUD_LOCATION=us-central1

# Routes the google-genai SDK and ADK to Vertex AI instead of AI Studio
GOOGLE_GENAI_USE_VERTEXAI=true

# ── Model Override (Optional) ─────────────────────────────────────────────────
# Default is gemini-2.5-flash. Available Vertex AI models:
#   gemini-2.5-flash          ← default, recommended
#   gemini-2.5-flash-lite     ← cheaper / faster
#   gemini-3-flash-preview    ← latest preview
#   gemini-3-pro-preview      ← most capable
# GEMINI_MODEL=gemini-2.5-flash
